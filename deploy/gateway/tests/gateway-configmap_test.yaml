suite: Gateway configmap
templates:
  - gateway-configmap.yaml
tests:
  - it: should set common settings
    set:
      twingate:
        network: "acme"
        host: "test"
      auditLog:
        flushInterval: "10s"
        flushSizeThreshold: 200
      containerPorts:
        https: 8000
        metrics: 9000
    asserts:
      - equal:
          path: data["config.yaml"]
          value: |-
            twingate:
              network: "acme"
              host: "test"

            port: 8000
            metricsPort: 9000

            auditLog:
              flushInterval: 10s
              flushSizeThreshold: 200

            tls:
              certificates:
                files:
                  - certificateFile: "/etc/gateway/tls/0/tls.crt"
                    privateKeyFile: "/etc/gateway/tls/0/tls.key"
  - it: should register all certificates
    set:
      twingate:
        network: "acme"
        host: "test"
      tls:
        certificates:
          autoGenerated:
            enabled: true
          secrets:
            - name: my-tls-secret
            - cert: "my-tls-cert"
              key: "my-tls-key"
              ca: "my-ca-cert"
    asserts:
      - matchRegex:
          path: data["config.yaml"]
          pattern: |-
            tls:
              certificates:
                files:
                  - certificateFile: "/etc/gateway/tls/0/tls.crt"
                    privateKeyFile: "/etc/gateway/tls/0/tls.key"
                  - certificateFile: "/etc/gateway/tls/1/tls.crt"
                    privateKeyFile: "/etc/gateway/tls/1/tls.key"
                  - certificateFile: "/etc/gateway/tls/2/tls.crt"
                    privateKeyFile: "/etc/gateway/tls/2/tls.key"
  - it: should error when network is not provided
    set:
      twingate:
        network: ""
    asserts:
      - failedTemplate:
          errorPattern: "twingate.network is required"
---
suite: Gateway configmap with Kubernetes
templates:
  - gateway-configmap.yaml
set:
  twingate:
    network: "acme"
  kubernetes:
    enabled: true
tests:
  - it: should render empty kubernetes block (in-cluster mode)
    asserts:
      - equal:
          path: data["config.yaml"]
          value: |-
            twingate:
              network: "acme"
              host: "twingate.com"

            port: 8443
            metricsPort: 9090

            auditLog:
              flushInterval: 5m
              flushSizeThreshold: 1e+06

            tls:
              certificates:
                files:
                  - certificateFile: "/etc/gateway/tls/0/tls.crt"
                    privateKeyFile: "/etc/gateway/tls/0/tls.key"

            kubernetes: {}
  - it: should include kubernetes upstream creds when provided (external mode for testing)
    set:
      kubernetes:
        enabled: true
        upstreams:
          - name: "external"
            bearerTokenFile: "/token"
            caFile: "/ca.crt"
    asserts:
      - equal:
          path: data["config.yaml"]
          value: |-
            twingate:
              network: "acme"
              host: "twingate.com"

            port: 8443
            metricsPort: 9090

            auditLog:
              flushInterval: 5m
              flushSizeThreshold: 1e+06

            tls:
              certificates:
                files:
                  - certificateFile: "/etc/gateway/tls/0/tls.crt"
                    privateKeyFile: "/etc/gateway/tls/0/tls.key"

            kubernetes:
              upstreams:
                - bearerTokenFile: /token
                  caFile: /ca.crt
                  name: external
  - it: should error when external Kubernetes upstream doesn't have bearer token nor bearer token file
    set:
      kubernetes:
        enabled: true
        upstreams:
          - name: "external"
            caFile: "/ca.crt"
    asserts:
      - failedTemplate:
          errorPattern: "'oneOf' failed"
  - it: should error when external Kubernetes upstream doesn't have CA file
    set:
      kubernetes:
        enabled: true
        upstreams:
          - name: "external"
            bearerToken: "token"
    asserts:
      - failedTemplate:
          errorPattern: "missing property 'caFile'"
---
suite: Gateway configmap with Resource
templates:
  - gateway-configmap.yaml
set:
  twingate:
    network: "acme"
tests:
  - it: should render kubernetes block when resource is enabled
    set:
      twingateOperator:
        kubernetesResource:
          enabled: true
    asserts:
      - equal:
          path: data["config.yaml"]
          value: |-
            twingate:
              network: "acme"
              host: "twingate.com"

            port: 8443
            metricsPort: 9090

            auditLog:
              flushInterval: 5m
              flushSizeThreshold: 1e+06

            tls:
              certificates:
                files:
                  - certificateFile: "/etc/gateway/tls/0/tls.crt"
                    privateKeyFile: "/etc/gateway/tls/0/tls.key"

            kubernetes: {}
  - it: should still render kubernetes block when kubernetes.enabled is false and resource is enabled
    set:
      twingateOperator:
        kubernetesResource:
          enabled: true
      kubernetes:
        enabled: false
    asserts:
      - equal:
          path: data["config.yaml"]
          value: |-
            twingate:
              network: "acme"
              host: "twingate.com"

            port: 8443
            metricsPort: 9090

            auditLog:
              flushInterval: 5m
              flushSizeThreshold: 1e+06

            tls:
              certificates:
                files:
                  - certificateFile: "/etc/gateway/tls/0/tls.crt"
                    privateKeyFile: "/etc/gateway/tls/0/tls.key"

            kubernetes: {}
---
suite: Gateway configmap with SSH
templates:
  - gateway-configmap.yaml
set:
  twingate:
    network: "acme"
  ssh:
    enabled: true
    gateway:
      username: "gateway"
tests:
  - it: should include SSH with manual CA
    set:
      ssh:
        ca:
          manual:
            privateKey: "my-private-key"
    asserts:
      - equal:
          path: data["config.yaml"]
          value: |-
            twingate:
              network: "acme"
              host: "twingate.com"

            port: 8443
            metricsPort: 9090

            auditLog:
              flushInterval: 5m
              flushSizeThreshold: 1e+06

            tls:
              certificates:
                files:
                  - certificateFile: "/etc/gateway/tls/0/tls.crt"
                    privateKeyFile: "/etc/gateway/tls/0/tls.key"

            ssh:
              gateway:
                username: "gateway"
                key:
                  type: "ed25519"
                hostCertificate:
                  ttl: "24h"
                userCertificate:
                  ttl: "5m"

              ca:
                manual:
                  privateKeyFile: "/etc/gateway/ssh/ca-private-key"
  - it: should include SSH with Vault CA
    set:
      ssh:
        ca:
          vault:
            address: "https://vault.example.com:8200"
            caBundle: "ca-cert"
            namespace: "test-namespace"

            mount: "ssh-gateway"
            role: "gateway"

            gatewayHostCA:
              mount: "ssh-gateway-host"
              role: "gateway-host"

            gatewayUserCA:
              mount: "ssh-gateway-user"
              role: "gateway-user"

            upstreamHostCA:
              mount: "ssh-upstream-host"
    asserts:
      - equal:
          path: data["config.yaml"]
          value: |-
            twingate:
              network: "acme"
              host: "twingate.com"

            port: 8443
            metricsPort: 9090

            auditLog:
              flushInterval: 5m
              flushSizeThreshold: 1e+06

            tls:
              certificates:
                files:
                  - certificateFile: "/etc/gateway/tls/0/tls.crt"
                    privateKeyFile: "/etc/gateway/tls/0/tls.key"

            ssh:
              gateway:
                username: "gateway"
                key:
                  type: "ed25519"
                hostCertificate:
                  ttl: "24h"
                userCertificate:
                  ttl: "5m"

              ca:
                vault:
                  address: "https://vault.example.com:8200"
                  namespace: "test-namespace"
                  mount: "ssh-gateway"
                  role: "gateway"
                  gatewayHostCA:
                    mount: "ssh-gateway-host"
                    role: "gateway-host"
                  gatewayUserCA:
                    mount: "ssh-gateway-user"
                    role: "gateway-user"
                  upstreamHostCA:
                    mount: "ssh-upstream-host"
  - it: should include SSH with Vault AppRole auth
    set:
      ssh:
        ca:
          vault:
            address: "https://vault.example.com:8200"
            auth:
              appRole:
                roleId: "my-role-id"
                secretId: "my-secret-id"
            mount: "ssh-gateway"
            role: "gateway"
    asserts:
      - equal:
          path: data["config.yaml"]
          value: |-
            twingate:
              network: "acme"
              host: "twingate.com"

            port: 8443
            metricsPort: 9090

            auditLog:
              flushInterval: 5m
              flushSizeThreshold: 1e+06

            tls:
              certificates:
                files:
                  - certificateFile: "/etc/gateway/tls/0/tls.crt"
                    privateKeyFile: "/etc/gateway/tls/0/tls.key"

            ssh:
              gateway:
                username: "gateway"
                key:
                  type: "ed25519"
                hostCertificate:
                  ttl: "24h"
                userCertificate:
                  ttl: "5m"

              ca:
                vault:
                  address: "https://vault.example.com:8200"
                  auth:
                    appRole:
                      mount: "approle"
                      roleID: "my-role-id"
                      secretIDFile: "/etc/gateway/vault/approle/secretId"
                  mount: "ssh-gateway"
                  role: "gateway"
  - it: should error when SSH Gateway username is empty
    set:
      ssh:
        gateway:
          username: ""
        ca:
          manual:
            privateKey: "my-private-key"
    asserts:
      - failedTemplate:
          errorPattern: "ssh.gateway.username is required"
  - it: should error when SSH is enabled but ca is omitted
    set:
      ssh:
        ca: null
    asserts:
      - failedTemplate:
          errorPattern: "at '/ssh': missing property 'ca'"
  - it: should error when SSH is enabled but neither CA backend is configured
    set:
      ssh:
        ca: {}
    asserts:
      - failedTemplate:
          errorPattern: "at '/ssh/ca': 'oneOf' failed, none matched"
  - it: should error when both manual and Vault config are provided
    set:
      ssh:
        ca:
          manual: {}
          vault: {}
    asserts:
      - failedTemplate:
          errorPattern: "at '/ssh/ca': 'oneOf' failed, subschemas 0, 1 matched"
  - it: should error when Vault auth is empty
    set:
      ssh:
        ca:
          vault:
            address: "https://vault.example.com:8200"
            role: "gateway"
            auth: {}
    asserts:
      - failedTemplate:
          errorPattern: "at '/ssh/ca/vault/auth': missing property 'appRole'"
  - it: should error when a Vault token is set under auth
    set:
      ssh:
        ca:
          vault:
            address: "https://vault.example.com:8200"
            role: "gateway"
            auth:
              token: "my-token"
    asserts:
      - failedTemplate:
          errorPattern: "at '/ssh/ca/vault/auth': additional properties 'token' not allowed"
---
suite: Gateway configmap with WebApp
templates:
  - gateway-configmap.yaml
set:
  twingate:
    network: "acme"
  webApp:
    enabled: true
tests:
  - it: should render empty webApp block when enabled without headers
    asserts:
      - equal:
          path: data["config.yaml"]
          value: |-
            twingate:
              network: "acme"
              host: "twingate.com"

            port: 8443
            metricsPort: 9090

            auditLog:
              flushInterval: 5m
              flushSizeThreshold: 1e+06

            tls:
              certificates:
                files:
                  - certificateFile: "/etc/gateway/tls/0/tls.crt"
                    privateKeyFile: "/etc/gateway/tls/0/tls.key"

            webApp: {}
  - it: should render webApp with custom headers
    set:
      webApp:
        requestHeaders:
          X-Groups: "{{groups}}"
          X-User: "{{username}}"
    asserts:
      - equal:
          path: data["config.yaml"]
          value: |-
            twingate:
              network: "acme"
              host: "twingate.com"

            port: 8443
            metricsPort: 9090

            auditLog:
              flushInterval: 5m
              flushSizeThreshold: 1e+06

            tls:
              certificates:
                files:
                  - certificateFile: "/etc/gateway/tls/0/tls.crt"
                    privateKeyFile: "/etc/gateway/tls/0/tls.key"

            webApp:
              requestHeaders:
                X-Groups: '{{groups}}'
                X-User: '{{username}}'
  - it: should not render webApp when disabled
    set:
      webApp:
        enabled: false
    asserts:
      - equal:
          path: data["config.yaml"]
          value: |-
            twingate:
              network: "acme"
              host: "twingate.com"

            port: 8443
            metricsPort: 9090

            auditLog:
              flushInterval: 5m
              flushSizeThreshold: 1e+06

            tls:
              certificates:
                files:
                  - certificateFile: "/etc/gateway/tls/0/tls.crt"
                    privateKeyFile: "/etc/gateway/tls/0/tls.key"
---
suite: Gateway configmap with TLS automation
templates:
  - gateway-configmap.yaml
set:
  twingate:
    network: "acme"
tests:
  - it: should render automation block
    set:
      tls:
        certificates:
          autoGenerated:
            enabled: false
        automation:
          enabled: true
          certificate:
            ttl: "24h"
            key:
              type: "ecdsa"
              bits: 256
          issuer:
            gcpPrivateCA:
              project: "twingate-prod"
              location: "us-east1"
              caPoolId: "gateway-ca"
    asserts:
      - equal:
          path: data["config.yaml"]
          value: |-
            twingate:
              network: "acme"
              host: "twingate.com"

            port: 8443
            metricsPort: 9090

            auditLog:
              flushInterval: 5m
              flushSizeThreshold: 1e+06

            tls:
              automation:
                certificate:
                  ttl: "24h"
                  key:
                    type: "ecdsa"
                    bits: 256
                issuer:
                  gcpPrivateCA:
                    project: "twingate-prod"
                    location: "us-east1"
                    caPoolID: "gateway-ca"
  - it: should fail on an unsupported key type
    set:
      tls:
        certificates:
          autoGenerated:
            enabled: false
        automation:
          enabled: true
          certificate:
            key:
              type: "ed25519"
          issuer:
            gcpPrivateCA:
              project: "twingate-prod"
              location: "us-east1"
              caPoolId: "gateway-ca"
    asserts:
      - failedTemplate:
          errorPattern: "at '/tls/automation/certificate/key/type': value must be one of 'ecdsa', 'rsa'"
  - it: should render no automation block when disabled
    set:
      tls:
        automation:
          enabled: false
          issuer:
            gcpPrivateCA:
              project: "twingate-prod"
              location: "us-east1"
              caPoolId: "gateway-ca"
    asserts:
      - notMatchRegex:
          path: data["config.yaml"]
          pattern: "automation:"
  - it: should fail when enabled is omitted
    set:
      tls:
        automation:
          enabled: null
    asserts:
      - failedTemplate:
          errorPattern: "at '/tls/automation': missing property 'enabled'"
  - it: should fail when enabled without an issuer
    set:
      tls:
        certificates:
          autoGenerated:
            enabled: false
        automation:
          enabled: true
    asserts:
      - failedTemplate:
          errorPattern: "at '/tls/automation/issuer': 'oneOf' failed, none matched"
  - it: should fail on an unknown issuer
    set:
      tls:
        certificates:
          autoGenerated:
            enabled: false
        automation:
          enabled: true
          issuer:
            invalid:
              name: "garbage"
    asserts:
      - failedTemplate:
          errorPattern: "at '/tls/automation/issuer': additional properties 'invalid' not allowed"
  - it: should fail when another issuer is also set
    set:
      tls:
        automation:
          enabled: true
          issuer:
            local:
              certificate: "my-ca-cert"
              privateKey: "my-ca-key"
            gcpPrivateCA:
              project: "twingate-prod"
              location: "us-east1"
              caPoolId: "gateway-ca"
    asserts:
      - failedTemplate:
          errorPattern: "at '/tls/automation/issuer': 'oneOf' failed, subschemas 0, 1 matched"
---
suite: Gateway configmap with local issuer TLS automation
templates:
  - gateway-configmap.yaml
set:
  twingate:
    network: "acme"
  tls:
    certificates:
      autoGenerated:
        enabled: false
tests:
  - it: should render
    set:
      tls:
        automation:
          enabled: true
          issuer:
            local:
              certificate: "my-ca-cert"
              privateKey: "my-ca-key"
    asserts:
      - equal:
          path: data["config.yaml"]
          value: |-
            twingate:
              network: "acme"
              host: "twingate.com"

            port: 8443
            metricsPort: 9090

            auditLog:
              flushInterval: 5m
              flushSizeThreshold: 1e+06

            tls:
              automation:
                certificate:
                  ttl: "24h"
                  key:
                    type: "ecdsa"
                issuer:
                  local:
                    certificateFile: "/etc/gateway/tls/ca/ca.crt"
                    privateKeyFile: "/etc/gateway/tls/ca/ca.key"
  - it: should fail when the local issuer is empty
    set:
      tls:
        automation:
          enabled: true
          issuer:
            local: {}
    asserts:
      - failedTemplate:
          errorPattern: "at '/tls/automation/issuer/local': 'oneOf' failed"
  - it: should fail when the local issuer supplies a certificate without its private key
    set:
      tls:
        automation:
          enabled: true
          issuer:
            local:
              certificate: "my-ca-cert"
    asserts:
      - failedTemplate:
          errorPattern: "at '/tls/automation/issuer/local': 'oneOf' failed"
  - it: should fail when the local issuer supplies a certificate and an existing Secret
    set:
      tls:
        automation:
          enabled: true
          issuer:
            local:
              certificate: "my-ca-cert"
              privateKey: "my-ca-key"
              secretName: "my-existing-secret"
    asserts:
      - failedTemplate:
          errorPattern: "at '/tls/automation/issuer/local': 'oneOf' failed, subschemas 0, 1 matched"
  - it: should fail on an unknown key
    set:
      tls:
        automation:
          enabled: true
          issuer:
            local:
              unknown: "garbage"
    asserts:
      - failedTemplate:
          errorPattern: "at '/tls/automation/issuer/local': additional properties 'unknown' not allowed"
---
suite: Gateway configmap with GCP Private CA TLS automation
templates:
  - gateway-configmap.yaml
set:
  twingate:
    network: "acme"
tests:
  - it: should render
    set:
      tls:
        certificates:
          autoGenerated:
            enabled: false
        automation:
          enabled: true
          issuer:
            gcpPrivateCA:
              project: "twingate-prod"
              location: "us-east1"
              caPoolId: "gateway-ca"
              issuingCertificateAuthorityId: "gateway-ca-1"
    asserts:
      - equal:
          path: data["config.yaml"]
          value: |-
            twingate:
              network: "acme"
              host: "twingate.com"

            port: 8443
            metricsPort: 9090

            auditLog:
              flushInterval: 5m
              flushSizeThreshold: 1e+06

            tls:
              automation:
                certificate:
                  ttl: "24h"
                  key:
                    type: "ecdsa"
                issuer:
                  gcpPrivateCA:
                    project: "twingate-prod"
                    location: "us-east1"
                    caPoolID: "gateway-ca"
                    issuingCertificateAuthorityID: "gateway-ca-1"
  - it: should fail when project is empty
    set:
      tls:
        certificates:
          autoGenerated:
            enabled: false
        automation:
          enabled: true
          issuer:
            gcpPrivateCA:
              project: ""
              location: "us-east1"
              caPoolId: "gateway-ca"
    asserts:
      - failedTemplate:
          errorMessage: tls.automation.issuer.gcpPrivateCA.project is required
  - it: should fail when location is empty
    set:
      tls:
        certificates:
          autoGenerated:
            enabled: false
        automation:
          enabled: true
          issuer:
            gcpPrivateCA:
              project: "twingate-prod"
              location: ""
              caPoolId: "gateway-ca"
    asserts:
      - failedTemplate:
          errorMessage: tls.automation.issuer.gcpPrivateCA.location is required
  - it: should fail when CA pool is empty
    set:
      tls:
        certificates:
          autoGenerated:
            enabled: false
        automation:
          enabled: true
          issuer:
            gcpPrivateCA:
              project: "twingate-prod"
              location: "us-east1"
              caPoolId: ""
    asserts:
      - failedTemplate:
          errorMessage: tls.automation.issuer.gcpPrivateCA.caPoolId is required
  - it: should fail when the required fields are missing
    set:
      tls:
        certificates:
          autoGenerated:
            enabled: false
        automation:
          enabled: true
          issuer:
            gcpPrivateCA: {}
    asserts:
      - failedTemplate:
          errorPattern: "at '/tls/automation/issuer/gcpPrivateCA': missing properties 'project', 'location', 'caPoolId'"
---
suite: Gateway configmap with upstream CA bundles
templates:
  - gateway-configmap.yaml
set:
  twingate:
    network: "acme"
tests:
  - it: should not render upstreamCABundles by default
    asserts:
      - notMatchRegex:
          path: data["config.yaml"]
          pattern: upstreamCABundles
  - it: should render a file per bundle
    set:
      upstreamCABundles:
        - configMapName: "corp-root-ca"
        - configMapName: "partner-ca"
          configMapKey: "bundle.pem"
        - pem: "my-upstream-ca"
    asserts:
      - equal:
          path: data["config.yaml"]
          value: |-
            twingate:
              network: "acme"
              host: "twingate.com"

            port: 8443
            metricsPort: 9090

            auditLog:
              flushInterval: 5m
              flushSizeThreshold: 1e+06

            tls:
              certificates:
                files:
                  - certificateFile: "/etc/gateway/tls/0/tls.crt"
                    privateKeyFile: "/etc/gateway/tls/0/tls.key"

            upstreamCABundles:
              - file: "/etc/gateway/upstream-ca-bundles/0/ca.crt"
              - file: "/etc/gateway/upstream-ca-bundles/1/ca.crt"
              - file: "/etc/gateway/upstream-ca-bundles/2/ca.crt"
  - it: should error when a ConfigMap bundle has an empty name
    set:
      upstreamCABundles:
        - configMapName: ""
    asserts:
      - failedTemplate:
          errorPattern: "at '/upstreamCABundles/0/configMapName': minLength: got 0, want 1"
  - it: should error when an inline PEM bundle is empty
    set:
      upstreamCABundles:
        - pem: ""
    asserts:
      - failedTemplate:
          errorPattern: "at '/upstreamCABundles/0/pem': minLength: got 0, want 1"
  - it: should error when an entry sets both a ConfigMap and a PEM bundle
    set:
      upstreamCABundles:
        - configMapName: "corp-root-ca"
          pem: "my-upstream-ca"
    asserts:
      - failedTemplate:
          errorPattern: "at '/upstreamCABundles/0': 'oneOf' failed, subschemas 0, 1 matched"
  - it: should error when a ConfigMap key is set without a name
    set:
      upstreamCABundles:
        - configMapKey: "bundle.pem"
    asserts:
      - failedTemplate:
          errorPattern: "at '/upstreamCABundles/0': properties 'configMapName' required, if 'configMapKey' exists"
  - it: should error when an inline PEM bundle sets an unknown key
    set:
      upstreamCABundles:
        - pem: "my-upstream-ca"
          invalid: "garbage"
    asserts:
      - failedTemplate:
          errorPattern: "at '/upstreamCABundles/0': additional properties 'invalid' not allowed"
  - it: should error when a ConfigMap bundle sets an unknown key
    set:
      upstreamCABundles:
        - configMapName: "corp-root-ca"
          invalid: "garbage"
    asserts:
      - failedTemplate:
          errorPattern: "at '/upstreamCABundles/0': additional properties 'invalid' not allowed"
